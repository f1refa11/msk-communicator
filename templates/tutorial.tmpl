<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Список туториалов</title>
    <!-- Подключите ваши стили здесь -->
    <style>
        .tutorial-list {
            list-style: none;
            padding: 0;
        }
        .tutorial-item {
            margin: 10px 0;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .tutorial-link {
            text-decoration: none;
            font-size: 18px;
            color: #333;
            display: block;
        }
        .tutorial-link:hover {
            background-color: #f0f0f0;
        }
        
    </style>
</head>
<body>
    <h1>Доступные туториалы</h1>
    
    {% if tutorials %}
        <ul class="tutorial-list">
        {% for tut in tutorials %}
            <li class="tutorial-item">
                <!-- Ссылка ведет на первую страницу туториала -->
                <a class="tutorial-link" href="/tutorials/{{ tut }}/1">📂 {{ tut }}</a>
            </li>
        {% endfor %}
        </ul>
    {% else %}
        <p>Туториалы пока не добавлены.</p>
    {% endif %}
    
    <a href="/">Назад на главную</a>
</body>
</html>
