<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>msk communicator</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body class="app-page">
    {% include "topbar.tmpl" %}

    <main class="page-shell settings-page">
        <h1 class="page-title">Настройки аккаунта</h1>
        <p class="page-note">Все поля и кнопки увеличены для удобства. Настройки сгруппированы по разделам.</p>

        <div class="settings-grid">
            <section class="card form-card">
                <h2>Имя пользователя</h2>
                <form action="/api/account/update_name" method="post" class="form-grid">
                    <label class="field">
                        <span class="field-label">Новое имя</span>
                        <input type="text" id="name" name="name" class="field-input" value="{{ user[2] }}" autocomplete="name" required>
                    </label>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary btn-lg">Сохранить имя</button>
                    </div>
                </form>
            </section>

            <section class="card form-card">
                <h2>Номер телефона</h2>
                {% if tel_status_message %}
                    <p class="status-text {{ 'status-text-error' if tel_status_type == 'error' else '' }}" role="status" aria-live="polite">{{ tel_status_message }}</p>
                {% endif %}
                <form action="/api/account/update_tel" method="post" id="account-tel-form" class="form-grid">
                    <label class="field">
                        <span class="field-label">Новый номер</span>
                        <input type="tel" id="account-tel" name="tel" class="field-input" value="{{ user[1] }}" autocomplete="tel" inputmode="tel" maxlength="18" aria-describedby="account-tel-hint account-tel-error" required>
                        <span id="account-tel-hint" class="field-hint">Используйте номер в формате +7 (900) 123-45-67.</span>
                        <span id="account-tel-error" class="field-error" aria-live="polite"></span>
                    </label>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary btn-lg">Сохранить номер</button>
                    </div>
                </form>
            </section>

            <section class="card form-card">
                <h2>Пароль</h2>
                <form action="/api/account/update_password" method="post" class="form-grid">
                    <label class="field">
                        <span class="field-label">Текущий пароль</span>
                        <input type="password" id="current_pwd" name="current_pwd" class="field-input" autocomplete="current-password" required>
                    </label>
                    <label class="field">
                        <span class="field-label">Новый пароль</span>
                        <input type="password" id="new_pwd" name="new_pwd" class="field-input" autocomplete="new-password" required>
                    </label>
                    <label class="field">
                        <span class="field-label">Повторите пароль</span>
                        <input type="password" id="new_pwd_confirm" name="new_pwd_confirm" class="field-input" autocomplete="new-password" required>
                    </label>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary btn-lg">Обновить пароль</button>
                    </div>
                </form>
            </section>

            <section class="card form-card">
                <h2>Выход из аккаунта</h2>
                <p class="page-note">Нажмите кнопку ниже, чтобы выйти из текущего аккаунта на этом устройстве.</p>
                <div class="form-actions">
                    <a class="btn btn-ghost btn-lg" href="/logout">Выйти из аккаунта</a>
                </div>
            </section>

            <section class="card form-card danger-card">
                <h2>Удалить аккаунт</h2>
                <p class="page-note">Действие невозможно отменить. Вы потеряете весь прогресс.</p>
                <form action="/api/account/delete" method="post" class="form-grid">
                    <label class="field checkbox-field">
                        <input type="checkbox" id="confirm_delete" name="confirm_delete">
                        <span class="field-label">Я понимаю, что аккаунт будет удалён навсегда</span>
                    </label>
                    <label class="field">
                        <span class="field-label">Пароль</span>
                        <input type="password" id="delete_pwd" name="delete_pwd" class="field-input" autocomplete="current-password" required>
                    </label>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-danger btn-lg">Удалить аккаунт</button>
                    </div>
                </form>
            </section>

        </div>
    </main>
</body>
</html>
