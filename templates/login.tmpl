
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>msk communicator</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body class="auth-page">
    {% include "topbar.tmpl" %}
    <main class="page-shell">
        <section class="card form-card">
            <h1 class="page-title">Вход в аккаунт</h1>
            <p class="page-note">Введите номер телефона и пароль. Шрифт и поля увеличены, чтобы их было легко прочитать.</p>
            {% if login_error %}
                <p class="status-text status-text-error" role="status" aria-live="polite">{{ login_error }}</p>
            {% endif %}
            <form action="/api/account/login" method="post" id="login_form" class="form-grid">
                <label class="field">
                    <span class="field-label">Номер телефона</span>
                    <input type="tel" id="login-tel" name="tel" class="field-input" placeholder="+7 (900) 123-45-67" autocomplete="tel" inputmode="tel" maxlength="18" aria-describedby="login-tel-hint login-tel-error" required>
                    <span id="login-tel-hint" class="field-hint">Используйте номер в формате +7 (900) 123-45-67.</span>
                    <span id="login-tel-error" class="field-error" aria-live="polite"></span>
                </label>
                <label class="field">
                    <span class="field-label">Пароль</span>
                    <input type="password" id="pwd" name="pwd" class="field-input" placeholder="Введите пароль" autocomplete="current-password" required>
                </label>
                <div class="form-actions">
                    <button form="login_form" type="submit" class="btn btn-primary btn-lg">Войти</button>
                    <a class="btn btn-ghost btn-lg" href="/register">Создать аккаунт</a>
                </div>
                <div class="form-helper">
                    <a href="/forgot">Забыли пароль?</a>
                    {% if test %}
                        <span class="status-text">{{ test }}</span>
                    {% endif %}
                </div>
            </form>
        </section>
    </main>
</body>
</html>
