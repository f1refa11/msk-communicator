<div class="max-video-page">
    <style>
        .max-video-page {
            display: grid;
            gap: 16px;
        }

        .max-video-title {
            margin: 0;
            font-size: clamp(24px, 2.8vw, 32px);
        }

        .max-video-note {
            margin: 0;
            color: #4a4f5c;
            font-size: 17px;
        }

        .max-video-player {
            width: 100%;
            max-width: 960px;
            border-radius: 14px;
            border: 2px solid #d4dbec;
            background: #000;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
        }
    </style>

    <h2 class="max-video-title">Видео: отправка сообщений в MAX</h2>
    <p class="max-video-note">Посмотрите ролик, затем переключитесь на вкладку «Симулятор» вверху страницы.</p>

    <video class="max-video-player" controls playsinline>
        <source src="/tutorials-assets/maxmessages/maxmessage.mp4" type="video/mp4">
        Ваш браузер не поддерживает воспроизведение видео.
    </video>
</div>
