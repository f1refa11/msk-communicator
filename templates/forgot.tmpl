<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>msk communicator</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body class="auth-page">
    {% include "topbar.tmpl" %}
    <main class="page-shell">
        <section class="card form-card">
            <h1 class="page-title">Сброс пароля</h1>
            <p class="page-note">Укажите имя и номер телефона, чтобы задать новый пароль.</p>

            <form action="/api/account/forgot_password" method="post" id="forgot_form" class="form-grid">
                <label class="field">
                    <span class="field-label">Имя в аккаунте</span>
                    <input type="text" id="name" name="name" class="field-input" placeholder="Например: Елена Ивановна" autocomplete="name" required>
                </label>
                <label class="field">
                    <span class="field-label">Номер телефона</span>
                    <input type="tel" id="tel" name="tel" class="field-input" placeholder="+7 999 985-25-23" autocomplete="tel" required>
                </label>
                <label class="field">
                    <span class="field-label">Новый пароль</span>
                    <input type="password" id="new_pwd" name="new_pwd" class="field-input" autocomplete="new-password" required>
                </label>
                <label class="field">
                    <span class="field-label">Повторите пароль</span>
                    <input type="password" id="new_pwd_confirm" name="new_pwd_confirm" class="field-input" autocomplete="new-password" required>
                </label>
                <div class="form-actions">
                    <button form="forgot_form" type="submit" class="btn btn-primary btn-lg">сбросить пароль</button>
                    <a class="btn btn-ghost btn-lg" href="/login">вернуться ко входу</a>
                </div>
                {% if status %}
                    <div class="status-text">{{ status }}</div>
                {% endif %}
            </form>
        </section>
    </main>
</body>
</html>
