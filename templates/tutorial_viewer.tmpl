<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>{{ tutorial_name }} - Страница {{ current_page }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .viewer-container {
            border: 2px solid black;
            padding: 20px;
            width: 80%;
            max-width: 800px;
            text-align: center;
        }
        .header {
            font-weight: bold;
            font-size: 24px;
            margin-bottom: 20px;
        }
        .nav-bar {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            margin-bottom: 20px;
        }
        .nav-btn {
            text-decoration: none;
            color: black;
            font-size: 30px; /* Размер стрелок */
            user-select: none;
        }
        .nav-btn.disabled {
            color: #ccc;
            pointer-events: none;
        }
        .page-number-box {
            border: 2px solid black;
            padding: 5px 15px;
            font-size: 20px;
        }
        .home-btn {
            border: 2px solid black;
            padding: 5px 15px;
            text-decoration: none;
            color: black;
            font-weight: bold;
        }
        .content-box {
            border: 2px solid red;
            padding: 10px;
            min-height: 300px;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .content-box img {
            max-width: 100%;
            max-height: 500px;
        }
    </style>
</head>
<body>

    <div class="viewer-container">
        <!-- Название тутора -->
        <div class="header">{{ tutorial_name }}</div>

        <!-- Навигация -->
        <div class="nav-bar">
            <!-- Стрелка влево -->
            {% if current_page > 1 %}
                <a href="/tutorials/{{ tutorial_name }}/{{ current_page - 1 }}" class="nav-btn">⬅️</a>
            {% else %}
                <span class="nav-btn disabled">⬅️</span>
            {% endif %}

            <!-- Номер страницы -->
            <div class="page-number-box">{{ current_page }}</div>

            <!-- Кнопка "на главную" -->
            <a href="/tutorials" class="home-btn">на главную</a>

            <!-- Стрелка вправо -->
            {% if current_page < total_pages %}
                <a href="/tutorials/{{ tutorial_name }}/{{ current_page + 1 }}" class="nav-btn">➡️</a>
            {% else %}
                <span class="nav-btn disabled">➡️</span>
            {% endif %}
        </div>

        <!-- Контент (красная рамка) -->
        <div class="content-box">
            <img src="{{ image_src }}" alt="Слайд туториала">
        </div>
    </div>

</body>
</html>
