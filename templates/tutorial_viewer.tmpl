<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/static/style.css">
    <title>{{ tutorial_title }} - Страница {{ current_page }}</title>
</head>
<body class="app-page tutorial-viewer-page">
    {% include "topbar.tmpl" %}
    {% set level_label = 'Расширенный' if tutorial_level == 'advanced' else 'Базовый' %}
    <main class="page-shell">
        <section class="viewer-card card">
            <header class="viewer-header">
                <div>
{#                    <div class="section-kicker">режим: {{ level_label }}</div>#}
                    <h1 class="page-title">{{ tutorial_title }}</h1>
                    {% if viewer_navigation == 'style-switch' and style_options %}
                        <p class="page-note">Выберите формат прохождения: видео или симулятор.</p>
                    {% else %}
                        <p class="page-note">Страница {{ current_page }} из {{ total_pages }}. Используйте крупные стрелки для перехода между шагами.</p>
                    {% endif %}
                </div>
                <div class="viewer-meta">
                    <span class="pill">{{ current_page }} / {{ total_pages }}</span>
                    <a class="btn btn-ghost btn-lg" href="{{ back_href or '/tutorials' }}">К списку курсов</a>
                </div>
            </header>

            {% if viewer_navigation == 'style-switch' and style_options %}
                <div class="viewer-style-switch">
                    {% for option in style_options %}
                        {% if option.page == current_page %}
                            <span class="btn btn-primary btn-lg viewer-style-active">{{ option.label }}</span>
                        {% else %}
                            <a href="/tutorials/{{ tutorial_name }}/{{ option.page }}{{ viewer_query or '' }}" class="btn btn-ghost btn-lg">{{ option.label }}</a>
                        {% endif %}
                    {% endfor %}
                </div>
            {% else %}
                <div class="viewer-nav">
                    {% if current_page > 1 %}
                        <a href="/tutorials/{{ tutorial_name }}/{{ current_page - 1 }}{{ viewer_query or '' }}" class="btn btn-ghost btn-lg">⬅️ Предыдущий шаг</a>
                    {% else %}
                        <span class="btn btn-ghost btn-lg disabled">⬅️ Предыдущий шаг</span>
                    {% endif %}

                    {% if current_page < total_pages %}
                        <a href="/tutorials/{{ tutorial_name }}/{{ current_page + 1 }}{{ viewer_query or '' }}" class="btn btn-primary btn-lg">Следующий шаг ➡️</a>
                    {% else %}
                        <span class="btn btn-primary btn-lg disabled">Это последняя страница</span>
                    {% endif %}
                </div>
            {% endif %}

            <div class="viewer-content">
                {{ content | safe }}
            </div>
        </section>
    </main>

</body>
</html>
